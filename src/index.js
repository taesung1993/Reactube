import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Component는 HTML을 반환하는 함수
// React 와 HTML의 조합을 JSX라고 부름
// jsx는 리액트에만 있음

ReactDOM.render(<App />, document.getElementById("main"));
